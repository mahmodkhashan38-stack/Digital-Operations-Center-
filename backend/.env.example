PORT=5000
MONGODB_URI=
JWT_SECRET=
JWT_EXPIRES_IN=1h

# Required only to run `npm run seed:system-admin` (DOC-31). Not used by the
# server itself. Set real values in your local .env - never commit them.
SYSTEM_ADMIN_EMAIL=
SYSTEM_ADMIN_PASSWORD=
SYSTEM_ADMIN_FULL_NAME=System Administrator
